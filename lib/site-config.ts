export const siteConfig = {
  name: 'WPX Studio OS',
  description: 'A production-ready operating layer for AI-assisted studio workflows.',
  url: process.env.NEXT_PUBLIC_APP_URL ?? 'http://localhost:3000'
} as const;
