import { Navbar } from "@/components/navigation/navbar";import { projects } from "@/data/projects";import { activities } from "@/data/activities";import { Card } from "@/components/ui/card";import { Button } from "@/components/ui/button";
export default function Dashboard(){return <><Navbar/><main className="container section"><p className="meta">Dashboard</p><h1 className="h2">Operational clarity for creative teams.</h1><div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))"}}>{["98 Lighthouse","60 Components","20 Templates","8 Projects"].map(s=><Card key={s} style={{padding:24}}><p className="meta">Metric</p><h2>{s}</h2></Card>)}</div><h2>Projects</h2><div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))"}}>{projects.map(p=><Card key={p.id} style={{padding:20}}><p className="meta">{p.status}</p><h3>{p.title}</h3><p className="muted">{p.description}</p><Button>Open</Button></Card>)}</div><h2>Activity feed</h2>{activities.map(a=><p className="pill" key={a.id}>{a.user}: {a.message}</p>)}</main></>}
