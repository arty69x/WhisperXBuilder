import { Navbar } from "@/components/navigation/navbar";import { CommandPalette } from "@/components/navigation/command-palette";import { Button } from "@/components/ui/button";import { Card } from "@/components/ui/card";import { components } from "@/data/components";import { projects } from "@/data/projects";
export default function Home(){return <><Navbar/><CommandPalette/><main><section className="container section reveal"><p className="meta">Emerald Night · Amber Dawn</p><h1 className="hero-title">A living creative operating system.</h1><p className="muted" style={{maxWidth:720}}>WHISPERX Builder combines architectural workspace structure, editorial typography, cinematic motion, and production-ready accessibility.</p><Button variant="primary">Open builder</Button> <Button>Press Cmd+K</Button></section><section className="container grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(260px,1fr))"}}>{["Application Preview Window","Builder Canvas Preview","Layer Panel + Floating Toolbar","Inspector Preview","AI Studio Preview","Dashboard Stats"].map((x,i)=><Card key={x} className="reveal" style={{padding:24,minHeight:220,animationDelay:`${i*60}ms`}}><p className="meta">0{i+1}</p><h3>{x}</h3><p className="muted">Designed with tokenized surfaces, hover lift, focus rings, keyboard semantics, and responsive composition.</p></Card>)}</section><section className="container section"><h2 className="h2">Editorial system, not a template.</h2><p className="muted">Browse {components.length}+ components, {projects.length} active projects, full marketplace flows, AI streaming, publish feedback, skeleton states, and empty states.</p></section></main><footer className="container section"><p className="meta">WHISPERX Builder © 2026</p></footer></>}
