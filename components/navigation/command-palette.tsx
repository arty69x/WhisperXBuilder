"use client";
import { useEffect, useState } from "react";import Link from "next/link";
export function CommandPalette(){const[open,setOpen]=useState(false);useEffect(()=>{const on=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setOpen(true)} if(e.key==="Escape")setOpen(false)};addEventListener("keydown",on);return()=>removeEventListener("keydown",on)},[]);if(!open)return null;return <div role="dialog" aria-modal="true" aria-label="Command palette" style={{position:"fixed",inset:0,zIndex:60,background:"rgba(0,0,0,.45)",display:"grid",placeItems:"start center",paddingTop:100}} onClick={()=>setOpen(false)}><div className="card" style={{width:"min(640px,92vw)",padding:16}} onClick={e=>e.stopPropagation()}><input autoFocus aria-label="Search commands" placeholder="Search commands, pages, components..." style={{width:"100%",height:56,border:0,background:"var(--soft)",color:"var(--fg)",borderRadius:16,padding:16}}/><div className="grid" style={{marginTop:12}}>{["Builder","Marketplace","AI Studio","Dashboard","Docs"].map(x=><Link className="pill" key={x} href={`/${x==='AI Studio'?'studio':x.toLowerCase()}`} onClick={()=>setOpen(false)}>Open {x}</Link>)}</div><p className="muted">Esc closes. Cmd/Ctrl+K opens. Arrow navigation follows browser focus order.</p></div></div>}
